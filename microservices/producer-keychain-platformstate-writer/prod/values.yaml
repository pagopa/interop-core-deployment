name: "interop-be-producer-keychain-platformstate-writer"
techStack: "nodejs"

serviceAccount:
  roleArn: "arn:aws:iam::697818730278:role/interop-be-producer-keychain-platformstate-writer-prod-es1"

service:
  create: false

configmap:
  KAFKA_CLIENT_ID: "{{.Values.namespace}}-pagopa-interop-producer-keychain-platformstate-writer"
  KAFKA_GROUP_ID: "{{.Values.namespace}}-producer-keychain-platformstate-writer"

deployment:
  env:
    TOPIC_STARTING_OFFSET: "earliest"
  envFromFieldRef:
    AWS_ROLE_SESSION_NAME: "metadata.name"
  envFromConfigmaps:
    AUTHORIZATION_TOPIC: "common-kafka.AUTHORIZATION_TOPIC"
    KAFKA_BROKERS: "common-kafka.KAFKA_BROKERS"
    TOKEN_GENERATION_READMODEL_TABLE_NAME_PLATFORM: "common-token-generation-read-model.TOKEN_READMODEL_PLATFORM_STATES_TABLE_NAME"
    TOKEN_GENERATION_READMODEL_TABLE_NAME_TOKEN_GENERATION: "common-token-generation-read-model.TOKEN_READMODEL_TOKEN_STATES_TABLE_NAME"
    PRODUCER_KEYCHAIN_PLATFORM_STATES_TABLE_NAME: "common-token-generation-read-model.PRODUCER_KEYCHAIN_PLATFORM_STATES_TABLE_NAME"
